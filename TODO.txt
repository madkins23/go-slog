TODO

* Warnings on test page show handler tags, not names
* Add "Guts" (nee "debug") page to "Other" section on root
* README.md in internal/data
  - discuss relationship between benchmark handler tag and warnings handler name
    - bench generates output
      - relationship for each handler tag name = handler name
        # Handler[SamberLogrus]="samber/slog-logrus"
      - <test function name>/<benchmark test name> <benchmark data>
      - some warnings
    - verification tests generate warnings
    - bench data parser consumes bench output
    - warning parser gets handler tag name = handler name output from bench parser
    - warning parser consumes verification test output
    - warning parser consumes benchmark warnings
* Check for package compilation
  - Utility to process entries from Go package-linkage whatever
    in order to create usable tree view.
* Is there a way to avoid crashing when updates to slog handlers blow up?
  - Currently this is an issue with zerolog 1.32 breaking phsym/zeroslog.

Notes

> List of goland:noinspection codes (doesn't always work):
  - https://github.com/JetBrains/qodana-profiles/blob/master/.idea/inspectionProfiles/qodana.recommended.full.xml
